# CRITICAL: Builder loop liveness restart blocked
- enforce-laws: `0 violations`
- heartbeat age sec: modernization `69627`, inference `68730`, sanhedrin `2`
- required restart channel blocked: `ssh localhost` not permitted in sandbox
- direct restart blocked: cannot write logs in sibling repos from this sandbox
- policy parity/security gates: intact (`secure-local`, quarantine/hash, IOMMU + Book of Truth, attestation/policy digest)
