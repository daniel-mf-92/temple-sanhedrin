# Cross-Repo Audit: Generated Artifact Hygiene Drift

Generated: 2026-04-28T17:50:24Z
Scope: TempleOS `/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS` and holyc-inference `/Users/danielmatthews-ferrero/Documents/local-codebases/holyc-inference`
Audit angle: Cross-repo invariant check

## Question

Do the two builder repos keep historical audit inputs clean of local runtime artifacts, generated bytecode, and deprecated backups that can distort retroactive LAWS.md checks?

## Evidence

- TempleOS tracks `automation/__pycache__/bookoftruth-smoke-dashboard.cpython-314.pyc` as a 17,185-byte generated Python bytecode artifact.
- TempleOS tracks `automation/sched-lifecycle-invariant-window-code-cq-depth-check.sh.deprecated.bak` as a 3,275-byte deprecated backup script. Its basename is 72 characters and 9 hyphen-separated tokens, over the identifier-compounding file-name limits.
- TempleOS `.gitignore` ignores `*.log`, build images, editor files, and Codex runtime lock/log folders, but it does not ignore `__pycache__/`, `*.pyc`, or `*.bak`.
- holyc-inference tracks `automation/codex-inference-loop.log` and `codex-inference-loop.log`, even though these are local loop runtime logs rather than source, spec, or reproducible benchmark artifacts.
- holyc-inference `.gitignore` contains only `automation/logs/`, so root-level and `automation/` loop logs remain commit-eligible.
- Read-only provenance checks show TempleOS introduced the pycache and deprecated backup in `5e92e74` (`value-not-noise reforms...`), while holyc-inference loop logs were repeatedly touched by checkpoint and iteration commits between 2026-04-20 and 2026-04-22.

Read-only commands used:

```text
git -C TempleOS ls-files | rg '(__pycache__|\.pyc$|\.bak$|\.log$)'
git -C holyc-inference ls-files | rg '(__pycache__|\.pyc$|\.bak$|\.log$)'
git -C TempleOS log --oneline -- automation/__pycache__/bookoftruth-smoke-dashboard.cpython-314.pyc automation/sched-lifecycle-invariant-window-code-cq-depth-check.sh.deprecated.bak
git -C holyc-inference log --oneline -- automation/codex-inference-loop.log codex-inference-loop.log
```

## Findings

1. WARNING: TempleOS has generated bytecode in version control.
   The tracked `.pyc` file is host-side automation output, not HolyC source or a durable audit specification. It does not breach Law 1 because it is outside core paths, but it adds non-source noise to historical scans and can change based on host Python version rather than project intent.

2. WARNING: TempleOS keeps a deprecated backup script that violates the measurable identifier-compounding limits.
   `sched-lifecycle-invariant-window-code-cq-depth-check.sh.deprecated.bak` exceeds both the 40-character filename cap and the 5-token cap. Because it is tracked, retroactive checks must keep distinguishing intentional source from retained deprecated artifacts.

3. WARNING: holyc-inference tracks local loop logs.
   `automation/codex-inference-loop.log` and `codex-inference-loop.log` contain checkout/status/lock/runtime messages, including transient dirty-worktree state. These lines are useful operational traces locally, but once committed they become stale historical evidence and can confuse deep audits that search for blocker strings or loop liveness outcomes.

4. INFO: The two repos have inconsistent ignore coverage for the same artifact classes.
   TempleOS blocks `*.log` but misses Python bytecode and backups. holyc-inference blocks only `automation/logs/`, so direct loop logs are not excluded. This is cross-repo drift in audit-surface hygiene, not a current guest-networking or runtime-language violation.

5. WARNING: Historical audit tooling needs an explicit source-vs-artifact filter.
   Existing retroactive audits often grep changed paths and changed lines for LAWS.md-sensitive terms. Without a shared ignore/filter profile, generated files, backup scripts, and local logs can inflate Law 4, Law 5, and Law 7 signals differently across the two builder repos.

## LAWS.md Impact

- Law 1 is not directly violated by these artifacts because the non-HolyC files are host-side or automation-adjacent, not new implementation code in core subsystem paths.
- Law 4 identifier-compounding enforcement is affected by the TempleOS deprecated backup path, which remains a measurable over-limit tracked file.
- Law 5 and Law 7 historical audits are affected because committed runtime logs and stale backup scripts can create false evidence of busywork, repeated blockers, or operational state.

## Recommended Remediation

- Add shared hygiene rules to both repos for `__pycache__/`, `*.pyc`, `*.bak`, and direct loop logs such as `codex-*-loop.log`.
- Remove generated bytecode and local loop logs from source control through normal builder-owned remediation, not from this audit sibling.
- Update retroactive audit scripts to classify tracked artifacts before scoring Law 4, Law 5, or Law 7 findings.
- Keep durable benchmark reports, policy evidence, and source-level specs tracked; keep transient process logs and generated interpreter outputs out of builder histories.
