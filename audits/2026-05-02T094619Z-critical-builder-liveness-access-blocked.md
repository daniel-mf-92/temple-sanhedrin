# CRITICAL: Builder Liveness Blocked

- TempleOS heartbeat stale >10m and holyc-inference heartbeat stale >10m.
- Restart attempts via `ssh ... localhost` failed: `Could not resolve hostname localhost: -65563`.
- Fallback `ssh ... 127.0.0.1` is blocked in this sandbox: `Operation not permitted`.
