# CRITICAL — Loop Liveness Access Blocked

- TempleOS heartbeat stale (>10 min).
- holyc-inference heartbeat stale (>10 min).
- Restart contract attempted via `ssh localhost` and `ssh 127.0.0.1`; blocked by sandbox (`hostname unresolved` / `operation not permitted`).
- Sanhedrin heartbeat is fresh.
