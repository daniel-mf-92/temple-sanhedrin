# Retroactive Commit Audit: 485af0ea41a239c8393542d6e0e2fc5944f30f53

Audit timestamp: 2026-04-29T07:08:51+02:00
Repo: `/Users/danielmatthews-ferrero/Documents/local-codebases/holyc-inference`
Commit: `485af0ea41a239c8393542d6e0e2fc5944f30f53`
Subject: `feat(inference): codex iteration 20260429-064100`
Audit angle: retroactive commit audit against `LAWS.md`

## Scope Reviewed

- `MASTER_TASKS.md`
- `tests/reference_q4_gpt2.py`
- `tests/test_reference_q4_gpt2.py`

## Findings

### WARNING: WS12-05 was marked complete without committing the default expected-output fixture

Evidence:
- `MASTER_TASKS.md` changes `WS12-05 Reference output generator: run llama.cpp on known prompts, save expected outputs` from unchecked to checked.
- The commit adds a generator and tests, but no `tests/fixtures/reference_q4_gpt2.json`.
- Running `python3 tests/reference_q4_gpt2.py --emit-json` exits with `ERROR: fixture not found at .../tests/fixtures/reference_q4_gpt2.json`.

Law impact:
- Law 5 North Star Discipline: the work is useful tooling, but the declared task includes saved expected outputs and the default path cannot emit one.

Assessment:
- This is not a runtime HolyC purity violation because the files live under `tests/`, which LAWS.md explicitly allows for Python/C validation scripts.
- It is a completion-evidence gap: the reference generator is present, but the reference output artifact is absent.

Recommended remediation:
- Commit a local, deterministic fixture for the default prompt/model/seed or leave WS12-05 unchecked until the fixture is captured.

### WARNING: Reference capture path permits arbitrary shell execution without local-only provenance guard

Evidence:
- `tests/reference_q4_gpt2.py` accepts `--capture-cmd`.
- `_capture_token` runs `subprocess.run(capture_cmd, shell=True, ...)`.
- The script stores `capture_cmd` and a trailing output excerpt, but does not reject network commands, remote services, package-manager invocations, or non-local model paths.

Law impact:
- LAWS.md allows host-side tests, but the hard safety requirement rejects network-dependent ecosystems or remote runtime services.
- For a reference-output generator, provenance matters because captured values can become promotion evidence.

Assessment:
- This is a warning, not a direct violation: the audited commit does not itself run a network command.
- The tool should fail closed for obvious remote/network capture commands or require a structured argv invocation with local path evidence.

Recommended remediation:
- Replace `shell=True` with structured argv input.
- Add local-only capture metadata: executable path, model file path, model hash, prompt ids, seed, and command exit status.
- Reject `curl`, `wget`, `ssh`, `http://`, `https://`, package-manager, and API-client captures.

### INFO: HolyC runtime purity remains intact

Evidence:
- The commit adds only `tests/reference_q4_gpt2.py` and `tests/test_reference_q4_gpt2.py`.
- LAWS.md explicitly permits Python/C validation scripts under `tests/`.
- No `src/` HolyC runtime file was modified.

Law impact:
- No Law 1 or integer-runtime Law 4 violation found.

### INFO: Added tests cover generator mechanics but not the default fixture contract

Evidence:
- `python3 tests/test_reference_q4_gpt2.py` exits 0.
- `./.venv/bin/python -m pytest -q tests/test_reference_q4_gpt2.py` reports `3 passed`.
- The tests use temporary fixtures and include a missing-fixture error case, but do not require the default committed fixture to exist.

Law impact:
- Useful host-side coverage, but incomplete for the WS12-05 "save expected outputs" contract.

## Validation Performed

- `python3 tests/reference_q4_gpt2.py --emit-json` -> fails because the default fixture is absent.
- `python3 tests/reference_q4_gpt2.py --fixture /tmp/ref-q4-gpt2-audit.json --update-fixture --set-token 42 --emit-json` -> succeeds.
- `python3 tests/test_reference_q4_gpt2.py` -> exits 0.
- `./.venv/bin/python -m pytest -q tests/test_reference_q4_gpt2.py` -> `3 passed` with a pytest cache write warning.
- `automation/check-no-compound-names.sh 485af0ea41a239c8393542d6e0e2fc5944f30f53` from the inference repo -> PASS.

## Verdict

Commit `485af0ea41a239c8393542d6e0e2fc5944f30f53` adds useful host-side reference tooling, but it prematurely marks WS12-05 complete without the default expected-output fixture and leaves capture provenance too open-ended for secure-local promotion evidence. Record as 2 warning findings.
