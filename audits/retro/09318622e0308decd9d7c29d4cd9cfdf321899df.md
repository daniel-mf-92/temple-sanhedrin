# Retroactive Commit Audit: 09318622e0308decd9d7c29d4cd9cfdf321899df

- Repo: holyc-inference
- Branch observed: `codex/holyc-gpt55-bench`
- Commit date: 2026-04-29T22:27:33+02:00
- Subject: `feat(inference): codex iteration 20260429-222128`
- Audit timestamp: 2026-04-30T02:30:58+02:00
- Audit angle: retroactive LAWS.md audit only; sibling repo was read-only.

Result: **2 findings.**

## Changed Surface

- Adds QEMU prompt benchmark exit-class summary telemetry in host-side `bench/qemu_prompt_bench.py`.
- Extends host-side CI smoke assertions in `bench/qemu_prompt_bench_ci_smoke.py`.
- Refreshes synthetic benchmark artifacts under `bench/results/`.
- No inference runtime `src/` HolyC files changed.

## Findings

### CRITICAL: Identifier-compounding violations in touched benchmark artifacts

`automation/check-no-compound-names.sh 09318622e0308decd9d7c29d4cd9cfdf321899df` reports five violations across three touched CSV artifact names:

- `bench/results/qemu_prompt_bench_prompt_efficiency_latest.csv` has a 42-character stem and six underscore-separated tokens.
- `bench/results/qemu_prompt_bench_prompt_rank_latest.csv` has six underscore-separated tokens.
- `bench/results/qemu_prompt_bench_prompt_variability_latest.csv` has a 43-character stem and six underscore-separated tokens.

The files are host-side generated artifacts, but the identifier-compounding ban applies to both builder agents and the commit modifies these paths again instead of retiring the noncompliant identifiers.

### WARNING: Synthetic fixture pass artifact still blurs real guest validation

The refreshed Markdown report records `Status: pass`, `Command air-gap OK: True`, and new exit-class OK counts while the same report records `/tmp/TempleOS.synthetic.img` with `Exists` set to `False` and identifies QEMU as `bench/fixtures/qemu_synthetic_bench.py`.

This is not an air-gap breach: the recorded command includes explicit `-nic none`. The Law 5 risk is evidence semantics. A fixture-backed benchmark pass can be misread as a real air-gapped TempleOS guest benchmark unless the artifact headline and machine-readable outputs distinguish `synthetic_fixture` from `real_qemu_guest`.

## Non-Findings

- Law 1 HolyC Purity: PASS. Python changes are confined to host-side `bench/`; no forbidden-language core runtime path was modified.
- Law 4 Integer Purity: PASS. No runtime tensor HolyC code changed, and no runtime float/FPU path was introduced.
- Air-gap hard safety: PASS. The committed QEMU command records `-nic none`; this audit did not execute QEMU or any VM.
- Queue self-padding: PASS. `MASTER_TASKS.md` was not touched.

## Evidence Reviewed

- `git show --stat --summary --find-renames 09318622e0308decd9d7c29d4cd9cfdf321899df`
- `git show --name-status 09318622e0308decd9d7c29d4cd9cfdf321899df`
- `git show --check 09318622e0308decd9d7c29d4cd9cfdf321899df`
- `automation/check-no-compound-names.sh 09318622e0308decd9d7c29d4cd9cfdf321899df`
- AST parse checks for `bench/qemu_prompt_bench.py` and `bench/qemu_prompt_bench_ci_smoke.py` at the audited commit.
- Targeted review of `bench/results/qemu_prompt_bench_latest.md` for synthetic fixture and air-gap evidence.
