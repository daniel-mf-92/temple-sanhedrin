# Retroactive Commit Audit: f247f4ea41c581d7585a4daab75f4d5137f11986

Audit timestamp: 2026-04-30T01:41:55+02:00
Repo: `/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS`
Commit: `f247f4ea41c581d7585a4daab75f4d5137f11986`
Parent: `e4543e10d3dc1c56ea6f5ce491ea1dfee54a1490`
Subject: `feat(modernization): codex iteration 20260430-012850`
Audit angle: retroactive commit audit against `LAWS.md`

## Scope Reviewed

- `MODERNIZATION/MASTER_TASKS.md`
- `automation/bookoftruth-nodelete-paths.sh`
- `automation/bookoftruth-nodelete-smoke.sh`
- Read-only corroboration from `Kernel/BookOfTruth.HC`, `Kernel/BookOfTruthSerialCore.HC`, and `Kernel/KExts.HC`

## Findings

### CRITICAL: WS13-18 is marked complete while public disable-style Book-of-Truth controls remain outside the guard

Evidence:
- The commit marks `WS13-18 No-delete proof: verify no code path exists to clear, truncate, or disable the ledger` complete in `MODERNIZATION/MASTER_TASKS.md`.
- The new `automation/bookoftruth-nodelete-paths.sh` only rejects `BookTruth...Clear` and `BookTruth...Truncate` externs in `Kernel/KExts.HC`; it does not reject `BookTruth...Disable`.
- Current `Kernel/KExts.HC` still exports `BookTruthSourceMaskDisable(I64 mask=-1,I64 source=BOT_SOURCE_KERNEL)`.
- Current `Kernel/BookOfTruth.HC` still implements that public function and returns a source mask with the selected Book-of-Truth source bit removed.
- This same mutable-control class was already identified in the Law 3 backfill as strict Law 3 risk, so marking WS13-18 complete without either remediation or explicit scope narrowing overstates compliance.

Law impact:
- Law 3 forbids adding or retaining a "disable logging" flag, config, or API as an acceptable completed no-delete state.
- Law 5 North Star Discipline is also implicated because the progress ledger claims the automation rejects "disable CLI exports", but the committed regex does not check the `Disable` class.

Recommended remediation:
- Extend the guard to fail on exported `BookTruth...Disable` symbols unless each symbol is explicitly allowlisted with a written explanation that it cannot disable ledger recording.
- Either remove/rename the public disable-style API from core Book-of-Truth surfaces or amend WS13-18 to state exactly which disable semantics are out of scope and why.
- Reopen WS13-18 until the guard and the completed-task claim align.

### WARNING: No-delete smoke is a narrow host static check, not a full proof that no clear/truncate/disable path exists

Evidence:
- `automation/bookoftruth-nodelete-smoke.sh` only runs `automation/bookoftruth-nodelete-paths.sh` and checks for `status=pass` plus `allowed_reclaim_memset=1`.
- The path script checks two files for selected destructive patterns: `Kernel/BookOfTruth.HC` and `Kernel/BookOfTruthSerialCore.HC`.
- It validates `BookTruthReclaimPage` by searching for guard tokens in the function text, not by proving control-flow dominance of those guards over the permitted `MemSet(bot_entries+idx,0,sizeof(CBookOfTruthEntry));`.
- No runtime proof, sealed-page tamper attempt, no-delete digest, serial mirror evidence, or fail-stop behavior is exercised by the new smoke.

Law impact:
- Law 3 demands that the Book of Truth cannot be cleared, truncated, overwritten, disabled, or made hash-chain-skippable.
- A static source grep is useful audit coverage, but it is not sufficient evidence for the completed task wording "verify no code path exists" without a broader source surface inventory and runtime/semantic checks.

Recommended remediation:
- Keep the static check, but rename the task evidence to "source-pattern guard" or add deeper checks before calling WS13-18 complete.
- Add explicit coverage for exported disable/config APIs, hash-chain bypass controls, serial exfiltration removal, and a negative test showing the guard fails on representative clear/truncate/disable fixtures.

## Positive Observations

- The commit adds host-side automation only; no TempleOS core source code was modified.
- No QEMU, VM, networking, package-manager, or remote-service surface was introduced.
- The new host scripts are Bash/Python automation outside core TempleOS paths, which is permitted by Law 1 exceptions.
- The new script names pass the current identifier-compounding checker.

## Validation Performed

- `git show --stat --summary --format=fuller f247f4ea41c581d7585a4daab75f4d5137f11986`
- `git diff e4543e10d3dc1c56ea6f5ce491ea1dfee54a1490..f247f4ea41c581d7585a4daab75f4d5137f11986 --check`
- `bash -n automation/bookoftruth-nodelete-paths.sh automation/bookoftruth-nodelete-smoke.sh`
- `REPO_DIR=/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS bash automation/bookoftruth-nodelete-smoke.sh`
- `bash automation/check-no-compound-names.sh f247f4ea41c581d7585a4daab75f4d5137f11986`
- `rg -n "BookTruth(ReclaimPage|.*(Clear|Truncate|Disable|Dump|Export)|serial|sealed|bot_count|MemSet\\(bot_entries)" Kernel/BookOfTruth.HC Kernel/BookOfTruthSerialCore.HC Kernel/KExts.HC`
- `git blame -L 1036,1036 -- Kernel/KExts.HC`
- `git blame -L 19342,19370 -- Kernel/BookOfTruth.HC`

QEMU compile validation was not run during this retroactive audit; no VM command was executed.

## Verdict

Commit `f247f4ea41c581d7585a4daab75f4d5137f11986` improves host static coverage for obvious Book-of-Truth deletion paths, but it prematurely closes WS13-18. The new guard does not actually reject disable-style exported APIs while the progress ledger claims that it does. Record as 2 findings: 1 critical and 1 warning.
