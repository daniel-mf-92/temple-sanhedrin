# Retroactive Commit Audit: d9c3b620dbe9cf8bde884ed11c8ec1df99a68e89

- Repo: `/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS`
- Branch observed: `codex/modernization-loop`
- Commit: `d9c3b620dbe9cf8bde884ed11c8ec1df99a68e89`
- Subject: `feat(modernization): codex iteration 20260429-162527`
- Author date: `2026-04-29 16:41:12 +0200`
- Audit angle: retroactive commit audit against `LAWS.md`

## Scope Reviewed

Changed files:

- `Kernel/BookOfTruth.HC`
- `Kernel/KExts.HC`
- `MODERNIZATION/MASTER_TASKS.md`
- `automation/bookoftruth-model-parse-smoke.sh`

Static validation performed:

- `git show --check d9c3b620dbe9cf8bde884ed11c8ec1df99a68e89`
- `bash -n automation/bookoftruth-model-parse-smoke.sh`
- `automation/check-no-compound-names.sh d9c3b620dbe9cf8bde884ed11c8ec1df99a68e89`

## Findings

### CRITICAL: Law 3 / Law 8 - parser gate state can change without a Book of Truth event

Evidence:

- `Kernel/BookOfTruth.HC:761` adds `BookTruthModelParseRun(..., Bool emit_event=TRUE)`.
- `Kernel/BookOfTruth.HC:12587-12591` mutates `parse_fmt`, `parse_ok`, `parse_mask`, `parse_fuzz`, and `parse_tsc`.
- `Kernel/BookOfTruth.HC:12598-12604` only records the parse result when `emit_event` is true.
- `Kernel/KExts.HC:108-110` exposes the same optional event flag through the public extern.

Impact:

The new API permits a caller to update parser-hardening trust state with `emit_event=FALSE`, leaving no append-only ledger record for that act. That is a direct conflict with Law 3's prohibition on disabling Book of Truth logging paths and Law 8's requirement that each act be recorded synchronously and close to the operation. The issue is especially material because the changed state participates in model promotion decisions.

Required remediation:

Remove the public logging bypass from this new parser gate path. Parse result state changes should append a Book of Truth event unconditionally, and failure to append should follow the existing fail-stop doctrine instead of silently accepting unrecorded state.

### WARNING: Law 5 - WS14-04 is marked complete without the promised malformed corpus or fuzz regression runner

Evidence:

- `MODERNIZATION/MASTER_TASKS.md:262` changes WS14-04 to checked: `Add parser hardening gate: malformed GGUF/safetensors corpus + fuzz regression runner`.
- `automation/bookoftruth-model-parse-smoke.sh` only checks for literal markers and function declarations with `grep -Fq`.
- The commit adds no malformed GGUF/safetensors fixtures, no fuzz corpus, and no regression runner that exercises parse inputs.
- `Kernel/BookOfTruth.HC:12682-12687` only blocks promotion when `parse_fmt != 0 && !parse_ok`; a model with no parse run can still promote after schema/verify checks.

Impact:

The commit adds useful HolyC control-plane fields, but the completed task statement overclaims the delivered behavior. The north-star value is weaker than the task status implies because the hardening gate is not mandatory and the validation script cannot catch parser regressions or malformed input acceptance.

Required remediation:

Either reopen WS14-04 as partially complete, or add a real host-side malformed-corpus/fuzz regression harness plus a mandatory parse-run requirement before trusted promotion.

## Non-Findings

- Law 1: no foreign implementation language was added under core TempleOS paths; the new non-HolyC file is host-side automation.
- Law 2: no guest networking stack, socket code, WS8 work, or QEMU command was added in this commit.
- Law 4 identifier compounding: `automation/check-no-compound-names.sh d9c3b620dbe9cf8bde884ed11c8ec1df99a68e89` passed.
- Law 10: no OS-image update path or QEMU drive configuration was added.
- Law 11: no remote Book of Truth viewing/export path was added.

## Verdict

2 findings: 1 critical, 1 warning.
