# Retroactive Commit Audit: c8723be7f09cf35bc74c3213ab164f13326324f0

- Repo: holyc-inference (`/Users/danielmatthews-ferrero/Documents/worktrees/holyc-gpt55`)
- Commit date: 2026-04-29T22:41:15+02:00
- Subject: `feat(inference): codex iteration 20260429-223654`
- Audit scope: retroactive diff review against `LAWS.md`; no source modifications.

## Summary

This commit adds expected-token gate coverage to the QEMU prompt benchmark and refreshes synthetic benchmark artifacts. Two findings were recorded.

## Findings

### CRITICAL: Law 10 generated QEMU benchmark artifacts show a writable drive argument

Evidence:
- `bench/results/qemu_prompt_bench_launches_latest.jsonl` records commands with `"-drive","file=/tmp/TempleOS.synthetic.img,format=raw,if=ide"`.
- The top-level command SHA stays tied to that argument and the artifact reports `command_airgap_ok: true`.

The artifacts prove `-nic none`, which satisfies the air-gap requirement, but they do not prove `readonly=on` for the OS image. Law 10 requires QEMU launch commands to include `-drive readonly=on`.

### CRITICAL: Law 4 identifier-compounding violation in refreshed prompt benchmark CSV names

Evidence:
- `bench/results/qemu_prompt_bench_prompt_efficiency_latest.csv` has basename length 46 and 6 underscore-separated tokens.
- `bench/results/qemu_prompt_bench_prompt_rank_latest.csv` has 6 underscore-separated tokens.
- `bench/results/qemu_prompt_bench_prompt_serial_output_latest.csv` has basename length 49 and 7 underscore-separated tokens.
- `bench/results/qemu_prompt_bench_prompt_variability_latest.csv` has basename length 47 and 6 underscore-separated tokens.

`LAWS.md` bans names longer than 40 characters and names with more than five hyphen/underscore-separated tokens.

## Checks

- HolyC/integer purity: changed Python files are host-side benchmark/test tooling; no runtime tensor HolyC path changed.
- Air-gap: generated commands include `-nic none`; no network device/backend evidence was found.
- Immutable image: fails as above due missing `readonly=on`.
