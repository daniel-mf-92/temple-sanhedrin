# Retroactive Commit Audit: 22fb6517f26bb09b8af40977ead5cd797858e9e4

- Repo: `TempleOS`
- Commit: `22fb6517f26bb09b8af40977ead5cd797858e9e4`
- Subject: `ci: install ripgrep on Host Smoke runner`
- Commit time: `2026-04-27T17:29:18+02:00`
- Audit time: `2026-04-27T17:59:31Z`

## Scope

Reviewed the diff for `.github/workflows/modernization-smoke.yml`.

## Findings

- **CRITICAL - Law 2 / network-dependent build step:** `.github/workflows/modernization-smoke.yml` adds `sudo apt-get update && sudo apt-get install -y ripgrep` before host smoke checks. LAWS.md lists network-dependent package managers or build steps as air-gap violations for the modernization agent. This is host CI rather than guest networking, but it still makes the guard depend on live package repositories before the air-gap policy can execute.

## Notes

- This violation was later remediated by commit `880b9b74ca6329d1fc5d551c0a97245f146ff72b`, which removed the network install and added a `grep` fallback.
- Law 1 HolyC purity: no TempleOS core implementation files are changed.
- Law 2 guest air-gap: no QEMU command or guest networking stack is introduced; the violation is specifically the CI network package install.
- Law 3, Law 8, Law 9, Law 10, Law 11: no Book of Truth, serial, hash-chain, fail-stop, installed image, or local-only access behavior is changed.
- Identifier compounding ban: changed file basename is below 40 characters and at or below 5 hyphen/underscore-separated tokens.
- Verification was static/read-only: `git show --name-status`, full diff review, `git show --check`, and filename/token scanning.
