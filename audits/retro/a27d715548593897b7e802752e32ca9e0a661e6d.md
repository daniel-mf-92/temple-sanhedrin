# Retroactive Commit Audit: a27d715548593897b7e802752e32ca9e0a661e6d

- Repo: `TempleOS`
- Commit: `a27d715548593897b7e802752e32ca9e0a661e6d`
- Subject: `feat(modernization): codex iteration 20260428-192941`
- Commit time: `2026-04-28T19:48:28+02:00`
- Audit time: `2026-04-28T20:15:21+02:00`
- Scope reviewed: `Kernel/BookOfTruth.HC`, `Kernel/KExts.HC`, `MODERNIZATION/MASTER_TASKS.md`, `automation/bookoftruth-ioport-allowlist-smoke.sh`
- Audit angle: retroactive commit audit against `LAWS.md`

## Findings

### CRITICAL: Law 3 disable-logging API introduced for Book of Truth I/O events

The commit exports `BookTruthIOPortSet(Bool on=TRUE)` through `Kernel/KExts.HC:94` and implements it by assigning `bot_io_log_enabled=on` in `Kernel/BookOfTruth.HC:1915-1920`. `BookTruthIOPortRecord` then returns without appending whenever that flag is false at `Kernel/BookOfTruth.HC:1891-1892`.

This creates an explicit runtime API for disabling a Book of Truth event source. Law 3 forbids adding any disable-logging flag, config, or API for the Book of Truth.

### CRITICAL: Law 9 I/O wrappers allow hardware I/O to complete without fail-stopping on ledger failure

`BookTruthInU8Log` performs `InU8(port)` first, calls `BookTruthIOPortRecord(...)`, ignores the returned `Bool`, and returns the read value at `Kernel/BookOfTruth.HC:1902-1906`. `BookTruthOutU8Log` performs `OutU8(port,val)`, then calls `BookTruthIOPortRecord(...)`, also ignoring the return at `Kernel/BookOfTruth.HC:1909-1912`.

Because `BookTruthIOPortRecord` can return `FALSE` when logging is disabled, and also returns the append result, these wrappers can continue execution after the log record is skipped or fails. Law 9 requires the machine to halt before continuing when the Book of Truth cannot record.

### WARNING: Law 5 north-star validation recorded as RED without a committed rationale

The iteration log entry in `MODERNIZATION/MASTER_TASKS.md:4180` says validation included `north-star-e2e RED`. The commit does add substantive HolyC code, but the newer Law 5 text requires iterations that fail or do not advance `automation/north-star-e2e.sh` to explain why the work is still on the north-star path. This commit records the RED result without that rationale.

## Non-findings

- Law 1 HolyC purity: core implementation changes are in `.HC`; the Python code is confined inside a host-side `automation/` smoke harness.
- Law 2 air-gap: no QEMU command is added or changed by this commit; no networking stack, socket, DNS, DHCP, HTTP, TLS, or WS8 networking work was introduced.
- Law 4 identifier compounding: `automation/check-no-compound-names.sh a27d715548593897b7e802752e32ca9e0a661e6d` passed.

## Verification Performed

```text
automation/check-no-compound-names.sh a27d715548593897b7e802752e32ca9e0a661e6d
check-no-compound-names: OK

bash automation/bookoftruth-ioport-allowlist-smoke.sh
[bookoftruth-ioport-allowlist-smoke] pass: total=4 decode_ok=3 decode_fail=1 allowed=2 blocked=1 in=1 out=2 last_port=3F8 last_val=55
```

No QEMU or VM command was executed during this audit.

## Verdict

Violations found. The commit advances I/O port ledger observability, but the new runtime disable API and post-I/O failure-tolerant wrappers violate the Book of Truth immutability and fail-stop requirements.
