# Retroactive Commit Audit: 0fbfde44cc675261df1f4051a0b6d101e4f4bd9a

Repo: `/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS`

Commit: `0fbfde44cc675261df1f4051a0b6d101e4f4bd9a`

Parent: `f0140c739ca1b5a44547a9241baa9fcdfec9ce42`

Author date: 2026-04-30 20:08:47 +0200

Subject: `feat(modernization): codex iteration 20260430-195657`

Audit angle: retroactive commit audit against `LAWS.md`. The TempleOS worktree was inspected read-only. No trinity source code was modified. No live liveness watching was performed. No QEMU/VM command was executed, and no WS8 networking task was executed.

## Scope Reviewed

Changed files:

- `Kernel/KExts.HC`
- `Kernel/KInts.HC`
- `MODERNIZATION/MASTER_TASKS.md`
- `automation/irq-wrap-smoke.sh`

The commit marks WS2-03 done, replaces direct `INT_FAULT` calls to `ExceptBookTruthTamperHook` and `Fault2` with `IRQFaultTamperHook` and `IRQFaultDispatch`, exports the new wrappers, and extends the IRQ wrapper smoke test to require the new indirection.

## Findings

### CRITICAL-001: Fault tamper logging is moved behind a wrapper seam in the interrupt path

Law: Law 8, Book of Truth Immediacy & Hardware Proximity.

`Kernel/KInts.HC` previously called `ExceptBookTruthTamperHook` directly from `INT_FAULT` after saving fault context. This commit changes that call to `IRQFaultTamperHook`, then implements `IRQFaultTamperHook` as a separate HolyC function that calls `ExceptBookTruthTamperHook`.

Evidence:

- `Kernel/KInts.HC:120` now calls `IRQFaultTamperHook` from `INT_FAULT`.
- `Kernel/KInts.HC:144-147` defines `IRQFaultTamperHook` as a wrapper around `ExceptBookTruthTamperHook`.
- `automation/irq-wrap-smoke.sh:29-31` now fails if `INT_FAULT` still calls `ExceptBookTruthTamperHook` directly.

Impact: Law 8 explicitly forbids software layers between hardware events and Book of Truth recording paths. A named `IRQFaultTamperHook` wrapper seam is an added software layer in the fault-tamper append path, and the smoke test codifies that drift by rejecting the direct call.

Required remediation: keep the Book of Truth tamper append on the direct interrupt path. If abstraction is needed for non-Book-of-Truth fault dispatch, it must not sit between the hardware fault and `ExceptBookTruthTamperHook`.

### WARNING-001: Commit marks WS2-03 complete while live validation is RED

Law: Law 5, North Star Discipline.

The progress entry marks WS2-03 done, but the recorded validation ends with `RED: preflight failed (missing automation/shared.img)`.

Evidence:

- `MODERNIZATION/MASTER_TASKS.md:84-87` marks `WS2-03 Introduce interrupt/context-switch abstraction wrappers` complete.
- `MODERNIZATION/MASTER_TASKS.md:4225` records the validation command and the RED preflight failure.

Impact: the shell syntax, wrapper smoke, and identifier-name checks pass, but the commit did not establish a clean QEMU compile or north-star run for a core interrupt-path change. That leaves the marked-complete status stronger than the available validation evidence.

Required remediation: either restore the missing local image and rerun the recorded QEMU-gated validation with explicit `-nic none`/read-only image guarantees, or downgrade the task status until clean validation evidence exists.

## Checks Performed

- `git show --stat --summary --format=fuller 0fbfde44cc675261df1f4051a0b6d101e4f4bd9a`
- `git show --name-status --format='' 0fbfde44cc675261df1f4051a0b6d101e4f4bd9a`
- `git show --format='' --unified=80 0fbfde44cc675261df1f4051a0b6d101e4f4bd9a -- Kernel/KInts.HC Kernel/KExts.HC MODERNIZATION/MASTER_TASKS.md automation/irq-wrap-smoke.sh`
- `bash -n automation/irq-wrap-smoke.sh automation/check-no-compound-names.sh automation/north-star-e2e.sh automation/qemu-compile-test.sh` -> PASS
- `REPO_DIR="$PWD" bash automation/irq-wrap-smoke.sh` -> `irq-wrap-smoke: PASS`
- `bash automation/check-no-compound-names.sh HEAD` -> `check-no-compound-names: OK`

## Pass Notes

- Law 1 HolyC purity: PASS. Core implementation remains HolyC/assembly in existing kernel files; the new shell code is host-side automation.
- Law 2 air-gap sanctity: PASS for this diff. No networking stack, NIC driver, socket code, or QEMU command is added. No QEMU command was executed during this audit.
- Law 3 Book of Truth immutability: PASS for deletion/disable behavior. The diff does not add clear, truncate, overwrite, or disable logic.
- Law 4 identifier compounding: PASS. `automation/check-no-compound-names.sh HEAD` reports OK.
- Law 5 meaningful progress: WARNING. The task is marked complete despite RED QEMU preflight validation.
- Law 6 no self-generated queue items: PASS. The commit marks an existing WS item complete and does not add new `CQ-` or `IQ-` queue lines.
- Law 8 Book of Truth immediacy/proximity: FAIL as CRITICAL-001.
- Law 9 fail-stop supremacy: not directly implicated; no log-failure continuation path is added.
- Law 10 immutable OS image: not directly implicated; no QEMU drive command or OS image mutation path is added.
- Law 11 local access only: PASS for this diff. No remote Book of Truth viewing or export path is added.
