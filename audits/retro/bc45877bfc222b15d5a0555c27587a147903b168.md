# Retroactive Commit Audit: bc45877bfc222b15d5a0555c27587a147903b168

Repo: `/Users/danielmatthews-ferrero/Documents/local-codebases/TempleOS`
Commit: `bc45877bfc222b15d5a0555c27587a147903b168`
Subject: `feat(modernization): codex iteration 20260430-225745`
Committed: 2026-04-30T23:07:33+02:00

## Scope

Retroactive LAWS.md audit of the commit that added Book of Truth DMA watch aggregate logging, scheduler tick wiring, a fixture/replay smoke harness, and marked WS13-10 complete.

Touched files:
- `Kernel/BookOfTruth.HC`
- `Kernel/KExts.HC`
- `Kernel/KTask.HC`
- `MODERNIZATION/MASTER_TASKS.md`
- `automation/bookoftruth-dma-watch-smoke.sh`
- `automation/gen-bookoftruth-log-fixture.sh`

## Findings

### WARNING: Law 8 drift from scheduler-periodic DMA watch logging

The commit adds `BookTruthDMATick()` and wires it through `TaskBookTruthDMATick()` inside `TaskKillDying()`. That means the new DMA watch entry is an aggregate emitted by a later scheduler maintenance path, not an entry composed in the same instruction sequence as the DMA event itself.

This is not evidence that existing `BookTruthDMARecord(...)` append calls were removed, and the commit still increments `bot_dma_bytes_total` inside `BookTruthDMARecord(...)`. The drift is that the new WS13-10 completion path depends on delayed counter sampling for the watch signal. Law 8 explicitly rejects log entries written in deferred callbacks or queued/batched paths when they are presented as hardware-proximity Book of Truth evidence.

Evidence:
- `Kernel/BookOfTruth.HC:82134` increments `bot_dma_bytes_total` inside `BookTruthDMARecord(...)`.
- `Kernel/BookOfTruth.HC:82316` defines `public Bool BookTruthDMATick(...)`.
- `Kernel/KTask.HC:1083` defines `TaskBookTruthDMATick()`.
- `Kernel/KTask.HC:1128` invokes `TaskBookTruthDMATick` from `TaskKillDying()`.

### WARNING: Law 5 completion proof overstates DMA monitoring coverage

The commit marks `WS13-10 Add DMA transfer monitoring` complete, but the implementation observes Book of Truth DMA counters and emits fixture/replay evidence. The smoke script checks for symbols and synthetic `D5` payload rows; it does not prove that real DMA controller activity, IOMMU events, or bus-mastering attempts are intercepted at the hardware boundary.

This is meaningful progress, but the completion marker is stronger than the evidence. A safer status would keep WS13-10 open until there is either a hardware-proximity DMA hook or a documented statement that this is counter telemetry rather than transfer interception.

Evidence:
- `MODERNIZATION/MASTER_TASKS.md:240` changes WS13-10 from unchecked to checked.
- `automation/bookoftruth-dma-watch-smoke.sh` supports fixture and replay modes and validates marker/status rows from a serial log.
- `automation/gen-bookoftruth-log-fixture.sh` adds a `dma-watch` synthetic fixture profile.

## Non-Findings

- Law 1: core changes are HolyC; shell changes are host-side automation.
- Law 2: the new smoke harness is fixture/replay only and starts no QEMU process. No WS8 networking task is executed.
- Law 3: the commit adds a DMA-watch enable flag, but it does not disable the Book of Truth itself or clear sealed entries.
- Law 4 identifier compounding: `automation/check-no-compound-names.sh bc45877b` reported `OK`.
- Whitespace: `git diff bc45877b^..bc45877b --check` reported no whitespace errors.

## Evidence Commands

- `git show --name-status --format=fuller bc45877b`
- `git show --numstat --format='' bc45877b`
- `git diff bc45877b^..bc45877b --check`
- `automation/check-no-compound-names.sh bc45877b`
- `git diff bc45877b^..bc45877b -- Kernel/BookOfTruth.HC Kernel/KExts.HC Kernel/KTask.HC automation/bookoftruth-dma-watch-smoke.sh automation/gen-bookoftruth-log-fixture.sh MODERNIZATION/MASTER_TASKS.md`
- `git grep -n -E "BookTruthDMAWatchSet|BookTruthDMATick|bot_dma_watch_on" bc45877b -- Kernel/BookOfTruth.HC Kernel/KExts.HC Kernel/KTask.HC`

## Verdict

Pass with warnings. Record 2 warning findings.
