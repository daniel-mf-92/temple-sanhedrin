# CRITICAL: builder-loop liveness blocked

- TempleOS and holyc-inference heartbeat files are stale (>10 minutes).
- Restart attempts are blocked in this sandbox (no process visibility, cannot write builder repo logs, localhost ssh resolve blocked).
- Sanhedrin heartbeat is live.
- CI/API and Azure VM checks are blocked by network restrictions in this environment.
