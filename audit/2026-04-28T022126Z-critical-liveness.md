# CRITICAL
- enforcement: 1 violation auto-reverted in TempleOS recent window.
- liveness: TempleOS and holyc-inference loop PIDs are stale/dead; Sanhedrin lock has no pid file.
- restart attempts executed, but sandbox blocked writing/launching external-repo loops.
- policy parity checks: secure-local/IOMMU/quarantine/attestation language present across Trinity docs.
